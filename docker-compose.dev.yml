version: '3.8'

services:
  vitaldsp-webapp-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: vitaldsp/webapp:0.2.1-dev
    container_name: vitaldsp-webapp-dev
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHONPATH=/app:/app/src
      - PYTHONUNBUFFERED=1
      - FLASK_ENV=development
      - VITALDSP_VERSION=0.2.1-dev
    volumes:
      - .:/app
      - ./uploads:/app/uploads
      - ./logs:/app/logs
    restart: unless-stopped
    networks:
      - vitaldsp-dev-network

networks:
  vitaldsp-dev-network:
    driver: bridge
